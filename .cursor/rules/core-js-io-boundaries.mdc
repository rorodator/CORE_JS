---
description: CORE_JS application boundary — owning services perform external IO
globs: **/*.js
alwaysApply: false
---

# Application service IO boundary

- External IO belongs to an owning registered service. HTTP, persistence, storage, external APIs, and other external-resource access must be encapsulated behind semantic `$svc(...)` operations.
- Components **must not perform IO directly**.
- Application bootstraps, controllers, managers, and other non-service orchestration code must also delegate external-resource access to the owning service rather than use IO primitives directly.
- Components express application intent through semantic operations such as `$svc('orders').submit(order)`.
- Endpoint paths, HTTP methods, transport payload names, protocol conventions, and technical response normalization belong to the owning service.
- Owning services may use lower-level primitives such as `$svc('ajax')`; these primitives are not general application APIs. Do not expose a generic `post(path, payload)` wrapper as a semantic service API.
- Services own coherent non-visual application capabilities, including central behavior that has only one current caller when the knowledge belongs to the capability.
- Components remain responsible for rendering, DOM interaction, focus, modals, visual notifications, routing UI, and local UI state.
- Do not move deterministic domain calculations into a client service when another layer is already their source of truth.

Details and examples: `ai-instructions/services.md`.
